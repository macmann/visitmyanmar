import {describe,expect,it} from 'vitest';import {advanceGameTime,attractionStatus,clamp,journeyObjective,newPlayer,progress,resolveAction,shouldUnlockBagan} from '../src/lib/rules';
describe('two-clock and gameplay rules',()=>{it('rolls game time into a new day',()=>expect(advanceGameTime(1,1380,120)).toEqual({day:2,minutes:60}));it('handles attraction hours',()=>{expect(attractionStatus(300)).toBe('CLOSED');expect(attractionStatus(1200)).toBe('CLOSING SOON');expect(attractionStatus(800)).toBe('OPEN')});it('clamps energy boundaries',()=>{expect(clamp(120,0,100)).toBe(100);expect(clamp(-2,0,100)).toBe(0)});it('resolves a completed sleep exactly once',()=>{const p=newPlayer(crypto.randomUUID());p.energy=10;p.majorState='SLEEPING';p.activeAction={id:'a',kind:'SLEEP',startedAt:new Date(0).toISOString(),completesAt:new Date(1).toISOString(),gameMinutes:120,energyRecovery:25};const done=resolveAction(p,new Date(2));expect(done.energy).toBe(35);expect(done.majorState).toBe('EXPLORING');expect(resolveAction(done,new Date(3)).energy).toBe(35)});it('resolves authoritative travel destination',()=>{const p=newPlayer(crypto.randomUUID());p.majorState='TRAVELLING';p.activeAction={id:'a',kind:'TRAVEL',startedAt:new Date(0).toISOString(),completesAt:new Date(1).toISOString(),gameMinutes:660,energyRecovery:0,destination:'BAGAN'};expect(resolveAction(p,new Date(2)).destination).toBe('BAGAN')});it('requires day two and core evidence for Bagan',()=>{const p=newPlayer(crypto.randomUUID());p.discoveries=['shwedagon','a','b','c','d','e'];p.foods=['mohinga'];p.photos=['pagoda_portrait'];p.talkedTo=['a','b','c','d','e'];expect(progress(p)).toBeGreaterThanOrEqual(30);expect(shouldUnlockBagan(p)).toBe(false);p.day=2;expect(shouldUnlockBagan(p)).toBe(true)});it('starts the guided journey at guesthouse check-in',()=>expect(journeyObjective(newPlayer('guide')).id).toBe('check_in'))});
